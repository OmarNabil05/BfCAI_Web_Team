/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",       // scan your HTML
    "./src/**/*.{js,ts}", // scan any JS in src (optional for later)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
